version: 2

updates:
  - package-ecosystem: composer
    directory: /
    schedule:
      interval: weekly
    groups:
      composer-dependencies:
        patterns:
          - '*'

  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      frontend-compatible:
        patterns:
          - '*'
        update-types:
          - minor
          - patch
    ignore:
      # Keep Node runtime and its type surface on the same supported major.
      - dependency-name: '@types/node'
        update-types:
          - 'version-update:semver-major'
      # typescript-eslint does not yet support the TypeScript 7 compiler API.
      - dependency-name: typescript
        versions:
          - '>=7.0.0'

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - '*'
